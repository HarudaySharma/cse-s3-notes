- also called **multidrop** connections.
- In these connections ***more than two devices share a common (single) link***. 
- In this **environment** the *capacity of channel* is *shared*, either ***[[spatially]] or [[ temporally]]***.

*multipoint*
![[multipoint.jpg]]